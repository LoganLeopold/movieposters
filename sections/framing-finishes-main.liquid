
  <div class="wrapper">
    <div class="grid">
      <div class="grid__item all--12 xlarge-up--10 push--xlarge-up--1 text-center">
        <div class="framing__header">
          <h1>{{ page.title }}</h1>
          <p>{{ page.content }}</p>
        </div>
      </div>
    </div>
  </div>
